def func2(list):
    try:
        max=list[0]
        min-max
        for i in range(len(list)):
            if(list[i]>max):
                max=list[i]
            if (list[i] < min):
                min = list[i]
        return tuple([min,max])
    except:
        print('Wrong list')
